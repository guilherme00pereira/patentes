import { Input } from 'antd';

const TextInput = () => {
    return (
        <div>
            <Input placeholder="Termo" style={{width: "300px"}} />
        </div>
    );
};

export default TextInput;